# Prometheus — Supervision des services

Cette capture présente l’état des services supervisés par Prometheus.

L’outil est accessible via :
http://localhost:9090

Prometheus permet de vérifier que les services sont correctement surveillés (statut UP).

Cela confirme l’intégration du monitoring dans le projet CryptoBot.
